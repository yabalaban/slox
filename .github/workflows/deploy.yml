name: Build and Deploy to GitHub Pages

on:
  push:
    branches: [ main, master, claude/* ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install SwiftWasm
        run: |
          set -ex
          curl -L -o swiftwasm.tar.gz "https://github.com/swiftwasm/swift/releases/download/swift-wasm-5.9.2-RELEASE/swift-wasm-5.9.2-RELEASE-ubuntu22.04_x86_64.tar.gz"
          mkdir -p $HOME/swiftwasm
          tar -xzf swiftwasm.tar.gz -C $HOME/swiftwasm --strip-components=1
          ls -la $HOME/swiftwasm/
          echo "$HOME/swiftwasm/usr/bin" >> $GITHUB_PATH
          $HOME/swiftwasm/usr/bin/swift --version

      - name: Install Carton
        run: |
          set -ex
          # Download carton binary directly
          curl -L -o carton "https://github.com/swiftwasm/carton/releases/download/1.1.3/carton-x86_64-unknown-linux-gnu"
          file carton
          chmod +x carton
          sudo mv carton /usr/local/bin/
          carton --version

      - name: Build WASM Bundle
        run: |
          set -ex
          carton bundle --product slox-wasm --wasm-optimizations size
          ls -la Bundle/
          mv Bundle/* web/ || true

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './web'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
